#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd)"

export GOCACHE="${ROOT_DIR}/.tooling/gocache"
export GOMODCACHE="${ROOT_DIR}/.tooling/gomodcache"

mkdir -p "${GOCACHE}" "${GOMODCACHE}"

exec "${ROOT_DIR}/.tooling/go/bin/go" "$@"
