# Copy to `.env` (which is gitignored) and fill in values.

# dev | prod | test
APP_ENV=dev

# Bind address
APP_ADDR=127.0.0.1:8080

# Public base URL (used for cookie security decisions; required in prod)
# Local dev should be http to avoid secure-cookie login loops.
APP_PUBLIC_URL=http://127.0.0.1:8080

# Where user avatars are stored (default: data/avatars)
APP_AVATAR_DIR=data/avatars

# Postgres DSN (required for auth/friends to work).
# This is read by scripts/migrate automatically (no extra export needed).
# This app uses the `mtgleader` role and `mtg` database.
# Local dev: sslmode=disable
# Prod: use sslmode=require (or verify-full with certs)
APP_DB_DSN=postgres://mtgleader:8yr3jctj@127.0.0.1:5432/mtg?sslmode=disable

# 32+ bytes recommended (required in prod). Used to HMAC-sign the session cookie value.
APP_COOKIE_SECRET=change-me-please-change-me-please-32

# OAuth/OIDC audiences for external sign-in.
# GOOGLE_WEB_CLIENT_ID=your-google-web-client-id.apps.googleusercontent.com
# APPLE_SERVICE_ID=com.example.mtg.leaderboard

# Firebase Cloud Messaging (optional).
# APP_FCM_PROJECT_ID=your-firebase-project-id
# APP_FCM_CREDENTIALS=/path/to/service-account.json

# Comma-separated allowlist of admin users (emails). When empty, admin UI is not mounted.
APP_ADMIN_EMAILS=lh@intagri.io

# Optional: create a default admin user on startup (if missing).
# The bootstrap email is also automatically added to `APP_ADMIN_EMAILS`.
APP_ADMIN_BOOTSTRAP_EMAIL=lh@intagri.io
APP_ADMIN_BOOTSTRAP_USERNAME=admin
APP_ADMIN_BOOTSTRAP_PASSWORD=admin

# Session TTL (Go duration)
APP_SESSION_TTL=720h

# debug | info | warn | error
APP_LOG_LEVEL=debug
